# App Shell

This folder is the V2 web/API shell.

Keep this layer thin:

- `api/` for transport
- `screens/` for top-level views
- `components/` for reusable UI pieces

Product truth should stay in `core/`.
