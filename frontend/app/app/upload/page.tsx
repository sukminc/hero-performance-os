export default function UploadPage() {
  return (
    <section className="page-card">
      <p className="eyebrow">Upload</p>
      <h1>Upload foundation comes in Phase 2.</h1>
      <p className="subtle">
        This route already exists so the authenticated shell can be tested now. The next task will connect GG packet upload,
        duplicate guard, and processing states.
      </p>
    </section>
  );
}
