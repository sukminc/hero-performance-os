export default function AccountPage() {
  return (
    <section className="page-card">
      <p className="eyebrow">Account</p>
      <h1>Account and plan state will live here.</h1>
      <p className="subtle">Billing and entitlement are out of scope for Phase 1, but this route is reserved now so later rollout does not require navigation churn.</p>
    </section>
  );
}
