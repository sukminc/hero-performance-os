export default function OperatorPage() {
  return (
    <main className="shell">
      <section className="page-card">
        <p className="eyebrow">Operator</p>
        <h1>Operator surface remains private.</h1>
        <p className="subtle">Middleware already expects explicit operator role gating before this route should be reachable in production.</p>
      </section>
    </main>
  );
}
